# Hardhat Workspace

This package contains the contract source, deploy scripts, tasks, and LayerZero configuration used by the template.

Key directories:

- `contracts/`
- `deploy/`
- `tasks/`
- `config/`
- `devtools/`
- `test/`

Use root scripts (for example `pnpm compile` or `pnpm hardhat ...`) from repository root.
